# Copy to .env.production before a production build, or pass build-args in Docker/CI.
# For local Docker builds you can still use host.docker.internal — override when you deploy.

VITE_API_URL=http://localhost:5000/api
VITE_STRIPE_PUBLISHABLE_KEY=
