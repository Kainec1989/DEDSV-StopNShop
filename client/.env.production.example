# Copy to .env.production before a production build, or pass build-args in Docker/CI.
# Do not commit .env.production with real URLs or keys.

VITE_API_URL=https://your-api-host.example.com/api
VITE_STRIPE_PUBLISHABLE_KEY=
